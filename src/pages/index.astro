---
// Redirect root to default language (English) with proper base path
const base = import.meta.env.BASE_URL;
const enPath = base.endsWith('/') ? `${base}en/` : `${base}/en/`;
return Astro.redirect(enPath, 302);
---
